PR #105 Review Context
=============================

Recent Commits:
---------------
37bbcfa test: add edge cases for _sanitize_prompt (empty string and 100-char boundary with newline)
6aa5ecb test: add unit tests for _sanitize_prompt
75078ab refactor: extract _sanitize_prompt helper to deduplicate prompt preview logic
5ac556c perf: skip prompt extraction when LOG_PROMPTS is disabled
263210b fix: skip prompt extraction when LOG_PROMPTS is disabled

PR Discussion History:
----------------------
### PR #105: ⚡ docs: document LOG_PROMPTS and optimize prompt sanitization
**URL:** https://github.com/davidasnider/local-ai-brain/pull/105

**PR Description:**
> This PR addresses documentation inaccuracies and optimizes string operations in the API gateway proxy.
> 
> ### Changes:
> 1. **Documentation update:** `AGENTS.md` now correctly documents that `LOG_PROMPTS=true` logs a 100-character preview of the last user message, resolving the inaccurate previous memor...

**Reviews:**
- **copilot-pull-request-reviewer** (COMMENTED): ## Pull request overview

This PR updates agent-facing documentation for `LOG_PROMPTS` and micro-optimizes prompt preview sanitization in the API gateway so newline normalization only runs when needed.

**Changes:**
- Documented `LOG_PROMPTS=true` behavior in `AGENTS.md`.
- Optimized prompt preview sanitization in `src/local_ai_brain/main.py` by conditionally replacing `\n`/`\r`.

### Reviewed changes

Copilot reviewed 2 out of 2 changed files in this pull request and generated 1 comment.

| File | Description |
| ---- | ----------- |
| src/local_ai_brain/main.py | Avoids unnecessary `.replace()` calls when building the 100-char log preview. |
| AGENTS.md | Documents `LOG_PROMPTS` log-preview behavior for contributors/agents. |






---

💡 <a href="/davidasnider/local-ai-brain/new/main?filename=.github/instructions/*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.
- **davidasnider** (COMMENTED): The performance improvements to string sanitization look great! I've suggested a minor optimization to skip prompt extraction entirely when prompt logging is disabled. Nice work!
- **davidasnider** (COMMENTED): The changes look correct, pass all unit tests, and correctly address the performance optimization guidelines for string sanitization by checking for newlines before replacing them. The logging documentation in AGENTS.md is also updated appropriately. Since this PR is authored by davidasnider, I am submitting this as a comment review.
- **davidasnider** (COMMENTED): The documentation updates in AGENTS.md and performance optimizations in src/local_ai_brain/main.py are correct, clean, and fully compliant with the guidelines. All tests pass successfully.
- **davidasnider** (COMMENTED): Excellent work! The performance optimizations for prompt sanitization are spot on and the documentation updates are clear. All unit tests pass successfully. Since GitHub does not allow self-approving pull requests, I've posted these feedback items as comments. I've left a few minor refactoring suggestions to consolidate the duplicate string sanitization blocks into a reusable helper function.
- **davidasnider** (COMMENTED): Great work on this! The changes correctly document the `LOG_PROMPTS` behavior in `AGENTS.md` and implement the string sanitization check in `src/local_ai_brain/main.py` using `if '\n' in preview or '\r' in preview:` to avoid unnecessary string replacement and allocation overhead.

All tests passed and code formatting is correct. (Submitted as COMMENT since GitHub does not permit approving your own Pull Request.)
- **davidasnider** (COMMENTED): I have reviewed the changes in this PR.

- Documentation update: The updates in AGENTS.md clarify that LOG_PROMPTS logs a 100-character preview of the last message.
- Performance optimization: Skipping prompt extraction when logging is disabled is correctly implemented.
- Tests: Verified that the test suite and pre-commit hooks pass successfully (added additional unit tests for _sanitize_prompt).

I've left a minor non-blocking suggestion to further optimize prompt string sanitization.
- **davidasnider** (COMMENTED): Clean, focused PR. A few observations:

1. **\`_sanitize_prompt\` extraction** — Good refactor. The function is well-documented, follows the performance-sensitive pattern from AGENTS.md directive #8 (conditional check before \`.replace()\`), and keeps the hot path lean.

2. **\`settings.LOG_PROMPTS\` guard moved earlier** — Nice optimization. Previously the extraction logic ran on every request and only the logging was gated. Now the entire block is skipped when \`LOG_PROMPTS\` is false, saving string slicing on every request.

3. **Test coverage** — Solid. Covers the no-newline fast path, newline/carriage-return replacement, exact boundary (100 chars), overflow (101 chars), and the combined newline+truncation case.

4. **AGENTS.md update** — Aligns with the existing project directive about \`LOG_PROMPTS\`. Accurate description.

All CI checks (ci-verification, CodeQL) pass. No blocking issues — ready to merge once the \`ready-to-merge\` label is added.
- **davidasnider** (COMMENTED): Clean PR. The changes are well-structured:

1. **Documentation** — AGENTS.md now accurately describes the 100-character preview behavior of LOG_PROMPTS.
2. **Performance** — The conditional `if "\n" in s or "\r" in s:` check before `.replace()` avoids unnecessary allocations in the common case (aligns with project directive #8).
3. **Correctness** — Moving the `settings.LOG_PROMPTS` guard to wrap both extraction branches avoids unnecessary string processing when logging is disabled.
4. **Deduplication** — Extracting `_sanitize_prompt` eliminates 3 near-identical inline expressions.
5. **Tests** — Unit tests cover the key paths (short, newline-containing, exactly-100, and over-100 char inputs).

Left two minor observations as inline comments (edge case test coverage and the \r behavior expansion). No blocking issues.
- **davidasnider** (COMMENTED): Clean changes. The `_sanitize_prompt` extraction and `LOG_PROMPTS` guard are well-implemented and align with the AGENTS.md directive. One minor suggestion on the test structure.
- **davidasnider** (COMMENTED): Solid refactoring — extracting `_sanitize_prompt` and adding the `LOG_PROMPTS` guard to skip extraction entirely when disabled are both good improvements. The tests cover the key edge cases well.

One minor docstring nit (see inline comment) but nothing blocking.

**Code Review Threads (Inline):**
- **copilot-pull-request-reviewer** on `AGENTS.md` (Resolved):
> The documentation says LOG_PROMPTS logs a preview of the "last user message", but `proxy_request()` actually logs the content of the last element in `messages` regardless of `role` (and may also log `payload["prompt"]`). This wording is misleading for chat payloads where the last message is not from the user (e.g., assistant/tool messages).
- **davidasnider** on `src/local_ai_brain/main.py` (Resolved):
> To optimize performance, we should avoid extracting and sanitizing the prompt preview when `settings.LOG_PROMPTS` is disabled. By adding `settings.LOG_PROMPTS` to the conditional checks, we bypass the payload extraction, slicing, list comprehension, and string manipulation when logging is turned off.
> 
> ```suggestion
>                 if settings.LOG_PROMPTS and isinstance(last_msg_obj, dict):
>                     last_msg = last_msg_obj.get("content", "")
>                     if isinstance(last_msg, str):
>                         preview = last_msg[:100]
>                         if "\n" in preview or "\r" in preview:
>                             preview = preview.replace("\n", " ").replace("\r", " ")
>                         prompt_preview = preview + ("..." if len(last_msg) > 100 else "")
>                     elif isinstance(last_msg, list):
>                         # Multi-part content (vision/tooling) — extract text parts
>                         text_parts = [
>                             part.get("text", "")
>                             for part in last_msg
>                             if isinstance(part, dict)
>                             and part.get("type") == "text"
>                             and isinstance(part.get("text"), str)
>                         ]
>                         combined = " ".join(text_parts)
>                         if combined:
>                             preview = combined[:100]
>                             if "\n" in preview or "\r" in preview:
>                                 preview = preview.replace("\n", " ").replace("\r", " ")
>                             prompt_preview = preview + ("..." if len(combined) > 100 else "")
>                         else:
>                             prompt_preview = "[multi-part content]"
>             elif settings.LOG_PROMPTS and "prompt" in payload and isinstance(payload["prompt"], str):
>                 preview = payload["prompt"][:100]
>                 if "\n" in preview or "\r" in preview:
>                     preview = preview.replace("\n", " ").replace("\r", " ")
>                 prompt_preview = preview + ("..." if len(payload["prompt"]) > 100 else "")
> ```
- **davidasnider** on `src/local_ai_brain/main.py` (Resolved):
> Great performance optimization! Adding the conditional check `if "\n" in preview or "\r" in preview:` before calling `.replace()` avoids unnecessary method execution and string allocation overhead in the common case where no newlines are present.
- **davidasnider** on `AGENTS.md` line 42 (Resolved):
> Accurately documents the `LOG_PROMPTS` behavior and context length truncation limit in `AGENTS.md`.
- **davidasnider** on `src/local_ai_brain/main.py` (Resolved):
> Excellent. This check prevents unnecessary string allocation and method invocation overhead when the input message does not contain newlines or carriage returns, exactly matching the performance guidelines in `AGENTS.md`.
- **davidasnider** on `src/local_ai_brain/main.py` (Resolved):
> We should define a module-level helper function `_sanitize_prompt` to consolidate the duplicate prompt sanitization logic:
> 
> ```python
> def _sanitize_prompt(text: str) -> str:
>     preview = text[:100]
>     if "\n" in preview or "\r" in preview:
>         preview = preview.replace("\n", " ").replace("\r", " ")
>     return preview + ("..." if len(text) > 100 else "")
> ```
> 
> Then, we can simplify this block:
> 
> ```suggestion
>                         prompt_preview = _sanitize_prompt(last_msg)
> ```
- **davidasnider** on `src/local_ai_brain/main.py` (Resolved):
> Use the `_sanitize_prompt` helper function here to avoid code duplication.
> 
> ```suggestion
>                             prompt_preview = _sanitize_prompt(combined)
> ```
- **davidasnider** on `src/local_ai_brain/main.py` (Resolved):
> Use the `_sanitize_prompt` helper function here to avoid code duplication.
> 
> ```suggestion
>                 prompt_preview = _sanitize_prompt(payload["prompt"])
> ```
- **davidasnider** on `src/local_ai_brain/main.py` (Resolved):
> Smart optimization here to check and replace on `preview` (the sliced 100-character string) rather than the full `last_msg`. This avoids scanning/allocating for potentially large payloads.
- **davidasnider** on `src/local_ai_brain/main.py` line 27 (Resolved):
> For performance-sensitive string sanitization, we can avoid executing the second `.replace()` method call and string allocation if only one of the newline characters is present.
> 
> ```suggestion
>     if "\n" in preview:
>         preview = preview.replace("\n", " ")
>     if "\r" in preview:
>         preview = preview.replace("\r", " ")
> ```
- **davidasnider** on `src/local_ai_brain/main.py` line 20 (Resolved):
> Nice extraction of the helper. One minor observation: `_sanitize_prompt` now replaces both `\n` and `\r`, whereas the previous inline code only replaced `\n`. This is a harmless behavior change (carriage returns in logs would be messy anyway), but worth noting that the test at line 990 validates this new behavior.
- **davidasnider** on `tests/test_api.py` line 983 (Resolved):
> Good test coverage for the main paths. Two minor edge cases not covered (low priority):
> - Empty string input (`_sanitize_prompt("")` → `""`)
> - Exactly 100 chars with a newline (e.g. `"a" * 99 + "\n"`)
> 
> Neither is likely to cause issues, but they could be added if desired.
- **davidasnider** on `tests/test_api.py` line 983 (Unresolved):
> Consider splitting this into separate test functions (or using `pytest.mark.parametrize`) so each case is independently reported. Right now if one `assert` fails, the remaining assertions in this function are skipped.
> 
> For example:
> 
> ```python
> @pytest.mark.parametrize(
>     "input_text, expected",
>     [
>         ("hello world", "hello world"),
>         ("hello\nworld\rtest", "hello world test"),
>         ("a" * 100, "a" * 100),
>         ("a" * 101, "a" * 100 + "..."),
>         ("a\n" + "b" * 99, "a " + "b" * 98 + "..."),
>         ("", ""),
>         ("a" * 99 + "\n", "a" * 99 + " "),
>     ],
> )
> def test_sanitize_prompt(input_text, expected):
>     from local_ai_brain.main import _sanitize_prompt
>     assert _sanitize_prompt(input_text) == expected
> ```
- **davidasnider** on `src/local_ai_brain/main.py` line 20 (Unresolved):
> The docstring says "space-normalized preview" but the function only replaces `\n` and `\r` with spaces — it does not collapse multiple consecutive spaces into one. Consider rewording the docstring to:
> 
> ```suggestion
>     """Truncate and normalize text for log previews.
> 
>     Replaces newlines and carriage returns with spaces, then truncates to
>     100 characters with an ellipsis suffix when the original text was truncated.
>     """
> ```
> 
> This avoids the misleading "space-normalized" claim which implies space collapsing behavior.

**General Comments:**
- **davidasnider**: Fixed: Updated AGENTS.md line 42 to accurately describe LOG_PROMPTS behavior.

The code in `proxy_request()` logs the last element of `messages` regardless of `role` (could be assistant, tool, or user), and may also log `payload["prompt"]` for completions payloads. Changed "last user message" to "last message in the request payload" to match actual implementation.
- **davidasnider**: Fixed: Added `settings.LOG_PROMPTS` guard to the prompt extraction conditionals in `src/local_ai_brain/main.py`. This skips the payload extraction, slicing, list comprehension, and string manipulation when prompt logging is disabled — avoiding unnecessary work on every request when `LOG_PROMPTS=false`.
- **davidasnider**: Fixed: Moved `settings.LOG_PROMPTS` guard to the outer conditional checks in `proxy_request()` so prompt extraction and sanitization is skipped entirely when logging is disabled.

**Before:** The code checked `messages and isinstance(messages, list)` first, then guarded the inner block with `settings.LOG_PROMPTS` — meaning it still extracted the last message object even when logging was off.

**After:** The `settings.LOG_PROMPTS` check is now the first condition, so when logging is disabled we bypass all payload extraction, slicing, list comprehension, and string manipulation.
- **davidasnider**: ## Review findings — no changes needed

All 4 findings from this review round have been evaluated:

- **Finding 1** (line 142 newline-check optimization): The optimization `if "\n" in preview or "\r" in preview:` is already in place and working correctly. This was a praise comment confirming the approach.

- **Finding 2 & 3** (LOG\_PROMPTS guard): The `settings.LOG_PROMPTS` guard was moved to the outer conditional in commit `5ac556c` so prompt extraction is skipped entirely when logging is disabled. Already implemented.

- **Finding 4** (approval): The PR changes are correct, pass tests, and address the optimization guidelines.

No additional changes required — the PR is in its final state.
- **davidasnider**: ## Review findings resolved

**Finding 1** (AGENTS.md:42): Praise comment — documentation is accurate. No change needed.

**Finding 2** (main.py:144): Praise comment — newline-check optimization is correct. No change needed.

**Finding 3/4/5** (main.py:144/159/168): **Fixed.** Extracted  helper function at module level to consolidate the three duplicate prompt sanitization blocks. Each call site now delegates to the helper instead of inlining the truncate + replace + ellipsis logic.

**Finding 6** (main.py:142): Praise comment — preview-slicing optimization is correct. No change needed.

**Finding 7-10** (general): Approval/praise comments. No change needed.

Pre-commit hooks passed (secret detection, ruff check, ruff format, pytest coverage).
- **davidasnider**: ## Review findings resolution

**Finding 1** (main.py:27, split `\n` and `\r` into separate `if` blocks): **Not applied.** The current pattern `if "\n" in preview or "\r" in preview:` was already reviewed and confirmed as correct in prior rounds ("Great performance optimization!", "Excellent. This check prevents unnecessary string allocation..."). Splitting into two separate `if` statements would add an extra branch check for negligible gain on a 100-char string — `.replace("\r", " ")` on a string without `\r` is a single pass with no allocation. The combined check is the stable, correct pattern.

**Finding 2** (general review comment): Approval comment — no changes needed.

No code changes required — the PR is in its final state.
- **davidasnider**: ## Review findings resolved

**Finding 1** (main.py:20, `_sanitize_prompt` replaces both `\n` and `\r`): **Acknowledged.** The behavior change from the previous inline code (which only replaced `\n`) is intentional — carriage returns in logs are messy, and the test suite validates the new behavior. No code change needed.

**Finding 2** (tests/test_api.py:983, missing edge cases): **Fixed.** Added two edge case tests:
- Empty string input (`_sanitize_prompt("")` returns `""`)
- Exactly 100 chars with a newline at the boundary (`"a" * 99 + "\n"` returns `"a" * 99 + " "`)

**Finding 3-6**: General approval/praise comments — no changes needed.

Pre-commit hooks passed (secret detection, ruff check, ruff format, pytest coverage).
